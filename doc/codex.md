# Codex CLI

This page is a placeholder for Codex CLI usage with AIStack.

Basic lifecycle commands:

```bash
./aistack cx install
./aistack cx register bash
./aistack cx launch -- --help
./aistack cx show-config
./aistack cx reset
```
